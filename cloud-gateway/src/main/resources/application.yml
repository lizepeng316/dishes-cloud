server:
  port: 9999
spring:
  application:
    name: gateway

  config:
    # gateway.yml
    import:
      - nacos:${spring.application.name}.yml
      - nacos:upload-service.yml

  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        namespace: et2512
      config:
        namespace: et2512